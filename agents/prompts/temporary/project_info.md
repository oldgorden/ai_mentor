# 项目路径与依赖

- 项目根目录: /home/lk/ai_mentor
- 代码中必须 `import sys; sys.path.insert(0, '/home/lk/ai_mentor')` 才能 import lib
- Python 解释器: sys.executable（不要硬编码 .venv 路径）
- 换项目时只改 `agents/prompts/temporary/` 目录下的文件
